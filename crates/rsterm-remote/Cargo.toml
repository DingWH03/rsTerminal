[package]
name = "rsterm-remote"
version.workspace = true
edition.workspace = true
description = "Remote status agent protocol and metrics for rsTerminal"
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
